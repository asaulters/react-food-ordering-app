import React, {useContext} from 'react';
import AvalibleMeals from '../Meals/AvalibleMeals';

const mealsCtx = props =>{

    // const mealCtx = useContext(AvalibleMeals);

    // return (
    //     <mealCtx.Provider>
    //         {props.children}
    //     </mealCtx.Provider>
    // )

}

export default mealsCtx;